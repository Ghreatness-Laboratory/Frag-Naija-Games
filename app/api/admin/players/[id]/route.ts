import { NextResponse } from 'next/server';import { createServiceSupabase, requireAdmin } from '@/lib/supabase/server';import { isUuid } from '@/lib/validators';
export async function PUT(req:Request,{params}:{params:Promise<{id:string}>}){if(!(await requireAdmin()).isAdmin)return NextResponse.json({error:'Forbidden'},{status:403});const {id}=await params;if(!isUuid(id))return NextResponse.json({error:'Invalid id'},{status:400});const b=await req.json();const patch={name:b.name,image_url:b.image_url,power:b.power,speed:b.speed,defense:b.defense,active:b.active,game:b.game};const {data,error}=await createServiceSupabase().from('players').update(patch).eq('id',id).select().single();return error?NextResponse.json({error:error.message},{status:400}):NextResponse.json({player:data});}
export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){if(!(await requireAdmin()).isAdmin)return NextResponse.json({error:'Forbidden'},{status:403});const {id}=await params;if(!isUuid(id))return NextResponse.json({error:'Invalid id'},{status:400});const {error}=await createServiceSupabase().from('players').update({active:false}).eq('id',id);return error?NextResponse.json({error:error.message},{status:400}):NextResponse.json({ok:true});}
